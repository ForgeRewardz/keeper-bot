{
  "$schema": "https://railway.com/railway.schema.json",
  "_comment": "Keeper serve-api service: receipt signing, bootstrap awards, /healthz. Pair with railway.worker.json for run-crons mode as a separate Railway service sharing the same image.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "/entrypoint.sh serve-api",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 1,
    "sleepApplication": false
  }
}
